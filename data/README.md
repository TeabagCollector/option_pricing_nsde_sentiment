# Data Directory

- **sample/**: Small sample data for quick runs (train_2000_seed42.csv, test_500_seed42.csv). Safe to commit.
- **train/**, **test/**: Full option data (gitignored). Use `OptionDataFetcher` or `prepare_sample_data.py` to generate.
